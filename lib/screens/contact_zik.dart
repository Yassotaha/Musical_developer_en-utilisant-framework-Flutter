import 'package:flutter/material.dart';

class ContactZik extends StatelessWidget {
  const ContactZik({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
